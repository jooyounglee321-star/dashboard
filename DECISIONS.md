# 프로젝트 결정 기록

---
## 2025-01-22 — 예산 조회 API: N+1 쿼리 제거를 통한 성능 최적화
**결정:** `list_budgets()` 함수에서 환율 조회 시 루프 내 개별 쿼리 호출 방식을 변경하여, 고유 통화를 먼저 수집한 후 단일 배치 조회로 변경하고 결과를 메모리 맵에 캐싱
**이유:** 예산 목록이 많을 경우 루프마다 `_get_rate()`를 호출하면 데이터베이스 쿼리 수가 O(N)이 되는 N+1 패턴 발생. 고유 통화만 먼저 조회한 후 메모리에서 빠르게 조회하면 DB 접근을 상수시간으로 줄일 수 있음
**대안:** (1) 원래 방식 - 루프 내 개별 쿼리 호출 (N+1 문제), (2) 루프 내 환율 조회 시 캐시 데코레이터 사용 (기존 함수 서명 변경 필요), (3) 환율을 API 응답에 포함시켜 프론트엔드에서 환산 (클라이언트 로직 복잡도 증가)
**파일:** routers/expense.py

---
## 2025-01-22 — FastAPI 세션 관리: 명시적 롤백 도입
**결정:** SQLAlchemy 세션 관리 시 요청 처리 중 예외 발생 시 명시적 `db.rollback()`을 호출하여 데이터 일관성을 보장하도록 변경
**이유:** 암묵적 롤백에 의존하는 것보다 예외 발생 시 명시적으로 롤백을 호출함으로써 데이터 일관성 보장이 더 명확하고 결정론적이 되며, 예외 처리 흐름이 예측 가능해짐
**대안:** (1) 원래 방식 - SQLAlchemy의 기본 동작에만 의존 (암묵적 롤백), (2) 예외를 잡아서 조용히 처리 (권장되지 않음), (3) 예외 발생 시 커밋 (데이터 일관성 문제 초래)
**파일:** database.py

---
## 2025-01-22 — React 비동기 함수: useCallback 메모이제이션 도입 및 에러 처리 강화
**결정:** ExpenseCard 컴포넌트의 데이터 로딩 함수(`loadCategories`, `loadExpenses`, `loadMonthly`)를 `useCallback`으로 감싸고, 토큰 접근 시 try-catch로 보호하며, useEffect 의존성 배열을 callback 함수를 포함하여 완성도 있게 작성
**이유:** (1) useCallback으로 메모이제이션하면 함수 참조 안정성이 보장되어 불필요한 Effect 재실행 방지, (2) 토큰 접근 시 try-catch를 추가하면 localStorage 접근 불가 상황(예: 프라이빗 브라우징)을 안전하게 처리, (3) 의존성 배열을 정확히 작성하면 React ESLint 규칙 준수 및 예측 가능한 재실행 보장
**대안:** (1) 원래 방식 - 함수를 컴포넌트 본문에 직접 선언 (매 렌더링마다 새로운 참조 생성), (2) 토큰 접근 시 에러 처리 없음 (특수 환경에서 런타임 에러 발생 가능), (3) 의존성 배열 무시 (exhaustive-deps 경고 무시, 미묘한 버그 위험)
**파일:** frontend/src/pages/index/ExpenseCard.jsx
