# 프로젝트 결정 기록

---
## 2026-06-08 21:09 — User 테이블에 식단 분석용 신체정보 컬럼 추가
**결정:** User 테이블에 birth_year(INTEGER), gender(VARCHAR(10)), height_cm(FLOAT), weight_kg(FLOAT) 네 컬럼을 nullable로 추가.
**이유:** 식단 분석 AI 기능에 필요한 사용자 신체정보를 저장하기 위해, 기존 User 테이블에 직접 추가하는 방식을 선택. 신체정보는 선택적(nullable)이므로 기존 사용자에게 영향 없음.
**대안:** (1) Health 또는 UserProfile 별도 테이블 생성 - 정규화된 설계이나 추가 JOIN으로 쿼리 복잡도 증가. (2) 키/몸무게를 dual unit으로 저장(cm/ft 또는 kg/lb) - 선택 후 단위 변환 불필요하나 저장 공간 증가. 최종 선택: 단순성과 기존 테이블 활용을 위해 User 테이블 확장 + 메트릭 단위 통일.
**파일:** C:\Users\Jason\Desktop\dashboard\main.py (lines 301-304)

---
## 2026-06-08 — 신체정보 단위 변환 전략: 항상 cm/kg로 DB 저장
**결정:** height_cm, weight_kg는 DB에 항상 SI 단위(cm, kg)로 저장. ft/lb 입력 시 프론트엔드에서 변환 후 저장.
**이유:** 단위가 통일되면 백엔드 로직이 단순해지고, 향후 AI 분석 연산에서 단위 혼용 오류를 원천 차단할 수 있음.
**대안:** ft/lb를 그대로 DB에 저장하고 API 응답 시 변환 — 프론트 로직이 단순해지나 백엔드 연산 복잡도 증가.
**파일:** frontend/src/pages/ProfilePage.jsx (handleSave 단위 변환 로직)

---
## 2026-06-09 14:52 — 프론트엔드에서 신체정보 단위 정규화
**결정:** ProfilePage.jsx의 handleSave 함수에서 사용자 입력 신체정보(키, 몸무게)를 항상 cm/kg으로 변환한 후 API로 전송. 키는 ft 입력 시 ×30.48, 몸무게는 lb 입력 시 ×0.4536으로 변환하며, 소수점 한자리까지 반올림.
**이유:** 데이터베이스에서 신체정보를 메트릭 단위(cm/kg)만 저장하기로 결정했으므로, 사용자가 입력하는 다양한 단위(ft/lb 등)를 프론트엔드에서 먼저 정규화하여 일관성 있는 데이터 저장 보장. 백엔드 단위 변환 로직 불필요.
**대안:** (1) 백엔드에서 단위 변환 - 백엔드 로직 증가. (2) 사용자 단위 저장 - 단위 혼재로 식단 분석 AI 처리 복잡화. (3) 프론트엔드에서 보관만, 백엔드 단위 변환 - 중복 처리. 최종 선택: 데이터 정규화는 입력 시점에서 수행하여 단순성 확보.
**파일:** C:\Users\Jason\Desktop\dashboard\frontend\src\pages\ProfilePage.jsx (lines 128-141)
