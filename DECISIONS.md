# 프로젝트 결정 기록

---
## 2025-01-16 — 고정 메모 UI를 위젯 외부 최상단에 배치

**결정:** 고정 메모(Pinned Notes) 기능을 위젯 드래그-드롭 레이아웃 시스템 외부에 배치하고, 헤더 아래 최상단에 항상 표시되도록 구현했다.

**이유:** 고정 메모는 사용자가 항상 접근할 수 있어야 하는 중요한 정보이므로 위젯 순서 변경의 영향을 받지 않아야 하며, 레이아웃 편집 모드에서도 위젯과 함께 드래그되지 않도록 하기 위함. UI 상단에 고정된 위치는 사용자 주의를 집중시킬 수 있다.

**대안:** 
- 고정 메모를 일반 위젯으로 취급하여 레이아웃 시스템 내에 포함 → 사용자가 순서 변경 시 고정 메모의 위치가 변할 수 있음
- 모달 또는 사이드바로 표시 → 상시 접근성 저하

**파일:** C:\Users\Jason\Desktop\dashboard\frontend\src\pages\index\IndexPage.jsx

---
## 2025-01-16 — 고정 메모 접기/펼치기 상태를 localStorage에 저장

**결정:** 고정 메모 UI의 펼침/접힘 상태를 localStorage에 저장하여 페이지 새로고침 후에도 유지하도록 구현했다.

**이유:** 사용자가 설정한 UI 상태를 기억함으로써 UX 연속성을 제공할 수 있으며, 데이터베이스 추가 쿼리 없이 가볍게 상태를 유지할 수 있다.

**대안:**
- 서버 데이터베이스에 저장 → 추가 API 호출 및 DB 쿼리 증가, 복잡도 상승
- 세션 스토리지 사용 → 탭 닫기 시 상태 소실
- 상태 유지 미구현 → 매 방문 시 초기화, UX 저하

**파일:** C:\Users\Jason\Desktop\dashboard\frontend\src\pages\index\IndexPage.jsx

---
## 2025-01-16 — PinnedMemoCard를 forwardRef로 변경하여 부모 컴포넌트의 직접 제어 가능하게 함

**결정:** PinnedMemoCard 컴포넌트를 forwardRef와 useImperativeHandle을 사용하여 리팩토링하고, 부모 컴포넌트에서 ref를 통해 `openAdd()` 메서드를 직접 호출할 수 있도록 변경했다.

**이유:** 부모 컴포넌트(IndexPage)에서 고정 메모 추가 폼을 프로그래매틱하게 열 수 있어야 하는데, 콜백 props 대신 ref 기반 imperative 호출이 더 직관적이고 제어 흐름이 명확하다. 또한 컴포넌트의 내부 상태 노출을 최소화할 수 있다.

**대안:**
- onOpenAdd 콜백 props 사용 → 상태 관리가 부모에 분산되고, props drilling 가능성
- 전역 상태 관리(Context/Redux) 사용 → 간단한 기능에 과도한 복잡도

**파일:** C:\Users\Jason\Desktop\dashboard\frontend\src\pages\index\PinnedMemoCard.jsx

---
## 2026-06-11 08:55 — 회원 목록 조회 엔드포인트에 역할 기반 접근 제어(RBAC) 추가

**결정:** GET /api/auth/users 엔드포인트에 관리자 권한 검증을 추가했다. FastAPI의 Depends 의존성 주입을 사용하여 get_current_user를 매개변수로 받고, 현재 사용자의 역할이 "admin"이 아니면 HTTP 403 Forbidden 예외를 발생시키도록 구현했다.

**이유:** 회원 목록은 민감한 정보이므로 관리자만 접근할 수 있어야 한다. FastAPI의 Depends 패턴은 프레임워크 네이티브이고, 코드 가독성이 좋으며, 다른 엔드포인트에서도 같은 패턴을 재사용할 수 있어 일관성이 높다.

**대안:**
- 데코레이터 패턴 (@require_admin) → 추가 코드 필요하지만 보일러플레이트 감소 가능
- 미들웨어 기반 인증 → 모든 엔드포인트에 일괄 적용되므로 선택적 권한 검증 어려움
- 개별 로직 → 각 엔드포인트마다 권한 검증 코드를 복제하여 유지보수 부담 증가

**파일:** C:\Users\Jason\Desktop\dashboard\routers\auth.py

---
## 2025-01-17 — 수입 목록 조회의 N+1 쿼리 문제 해결: 배치 조회 + 캐싱

**결정:** 수입 목록 조회 엔드포인트에서 카테고리/소카테고리 정보 로드 시 발생하는 N+1 쿼리 문제를 해결하기 위해, 배치 조회(batch query) 패턴을 도입했다. 반복문 실행 전에 필요한 모든 카테고리 ID를 수집하여 한 번의 SQL IN 쿼리로 조회하고, 결과를 딕셔너리에 캐싱하여 렌더링 루프 내에서 메모리 조회만 수행하도록 변경했다.

**이유:** 이전 구현은 각 수입 항목마다 `db.query(ExpenseCategory).get(cat_id)` 호출이 발생하여, 수입이 N개일 때 최소 N번의 카테고리 테이블 쿼리가 실행되는 O(N) 성능 문제가 있었다. 배치 조회 패턴은 쿼리 횟수를 1회(+ 초기 목록 쿼리)로 줄이므로 O(1) 성능을 달성하며, 데이터베이스 부하를 크게 감소시킬 수 있다. 또한 메모리 내 딕셔너리 조회는 거의 비용이 없으므로 전체 응답 시간이 단축된다.

**대안:**
- SQLAlchemy 관계 로딩 최적화(joinedload/selectinload) → ORM 수준에서 자동 최적화하지만 코드 가독성이 낮아질 수 있음
- N+1 쿼리 허용 → 데이터 증가 시 응답 시간이 선형으로 악화
- 단일 JOIN 쿼리로 통합 → 결과 중복(카테고리당 여러 행)으로 인해 클라이언트 필터링 필요

**파일:** C:\Users\Jason\Desktop\dashboard\routers\income.py

---
## 2026-06-11 08:58 — 지출 목록 조회의 N+1 쿼리 문제 해결: 배치 조회 + 캐싱

**결정:** 지출 목록 조회 엔드포인트에서 카테고리/소카테고리 정보 로드 시 발생하는 N+1 쿼리 문제를 해결하기 위해, 배치 조회(batch query) 패턴을 도입했다. 새로운 헬퍼 함수 `_build_cat_map(rows, db)`를 추가하여 반복문 실행 전에 필요한 모든 카테고리 ID를 수집하고, 한 번의 SQL IN 쿼리로 조회한 후 딕셔너리에 캐싱하도록 구현했다. `_expense_dict()` 함수에 선택적 `cat_map` 매개변수를 추가하여, 기존 호출 방식(개별 쿼리)과 최적화된 호출 방식(배치 맵 사용) 모두를 지원한다.

**이유:** 이전 구현은 각 지출 항목마다 `db.get(ExpenseCategory, category_id)` 호출이 발생하여, 지출이 N개일 때 최소 N번(또는 그 이상)의 카테고리 테이블 쿼리가 실행되는 O(N) 성능 문제가 있었다. 배치 조회 패턴은 쿼리 횟수를 1회(+ 초기 목록 쿼리)로 줄이므로 O(1) 성능을 달성하며, 데이터베이스 부하를 크게 감소시킬 수 있다. 또한 메모리 내 딕셔너리 조회는 거의 비용이 없으므로 전체 응답 시간이 단축된다.

**대안:**
- SQLAlchemy 관계 로딩 최적화(joinedload/selectinload) → ORM 수준에서 자동 최적화하지만 코드 가독성이 낮아질 수 있음
- N+1 쿼리 허용 → 데이터 증가 시 응답 시간이 선형으로 악화
- 단일 JOIN 쿼리로 통합 → 결과 중복(카테고리당 여러 행)으로 인해 클라이언트 필터링 필요

**파일:** C:\Users\Jason\Desktop\dashboard\routers\expense.py

---
## 2025-01-17 — _group_by_category() 함수에 N+1 쿼리 최적화 적용
**결정:** `_group_by_category()` 함수에 선택적 `cat_map` 매개변수를 추가하고, 호출 시 배치 조회된 카테고리 맵을 재사용할 수 있도록 변경했다. 함수 내부에서 cat_map이 None일 경우 자동으로 `_build_cat_map(rows, db)` 호출하여 배치 조회를 수행한다.

**이유:** 카테고리별 집계 함수 내에서 개별 카테고리 조회로 인한 N+1 쿼리 문제를 해결하기 위함. 반복문 내 `db.get(ExpenseCategory, e.category_id)` 호출을 `cat_map.get(e.category_id)` 메모리 조회로 변경하여 데이터베이스 쿼리 횟수를 O(N)에서 O(1)로 단축했다.

**대안:**
- 함수에 배치 조회 로직 내장 → 매번 중복된 ID 수집 로직이 필요
- 호출자가 모든 배치 조회를 책임 → 함수 재사용성 감소, 호출 코드 복잡도 증가
- SQLAlchemy 관계 로딩 최적화 → ORM 수준 복잡도 증가, 유지보수 어려움

**파일:** C:\Users\Jason\Desktop\dashboard\routers\expense.py

---
## 2026-06-11 08:59 — expense_stats 엔드포인트에 배치 조회 최적화 적용
**결정:** `/stats` 엔드포인트의 `expense_stats()` 함수에서 카테고리별 집계 시 N+1 쿼리 문제를 해결하기 위해 배치 조회 패턴을 적용했다. `_split_income_expense(rows)` 호출 후 `cat_map = _build_cat_map(rows, db)`를 실행하여 카테고리 맵을 미리 구성하고, `_group_by_category()` 호출 시 `cat_map=cat_map` 인자를 전달하도록 변경했다.

**이유:** `expense_stats()` 함수는 월별 통계를 위해 `_group_by_category(rows, db, lang, expense_type="expense")`를 호출하는데, 이 함수가 내부에서 각 행의 카테고리를 개별 조회하면 O(N) 쿼리가 발생한다. 미리 배치 조회된 `cat_map`을 재사용하면 추가 쿼리 없이 메모리 조회만으로 처리되므로 O(1) 성능을 달성한다. 동일한 최적화 패턴을 여러 통계 엔드포인트에 일관되게 적용하여 성능 및 유지보수성을 높인다.

**대안:**
- 개별 쿼리 허용 → 카테고리 수만큼 추가 데이터베이스 요청 발생
- 함수 내부에서 자동 배치 조회 → 이미 다른 곳에서 배치 조회한 결과를 중복으로 로드할 가능성
- SQLAlchemy 정크로딩 → ORM 복잡도 증가, 문서화 필요

**파일:** C:\Users\Jason\Desktop\dashboard\routers\expense.py

---
## 2026-06-11 08:59 — list_expenses() 엔드포인트에 배치 조회 최적화 적용

**결정:** `list_expenses()` 함수에서 지출/수입 항목들의 카테고리 정보를 로드할 때 배치 조회 패턴을 적용했다. 쿼리 결과를 받은 직후 `cat_map = _build_cat_map(rows, db)`를 호출하여 필요한 모든 카테고리를 한 번에 조회한 후, 렌더링 루프에서 `_expense_dict(e, db, lang, cat_map=cat_map)`으로 미리 구성된 맵을 전달하도록 변경했다.

**이유:** `list_expenses()` 함수는 사용자가 많은 수의 지출 항목을 조회할 수 있는 주요 엔드포인트이다. 이전 구현에서는 각 항목마다 카테고리 정보를 개별 조회하여 O(N) 쿼리 문제가 발생했다. 배치 조회 패턴을 적용함으로써 쿼리 횟수를 O(1)로 단축하여 대량의 지출 항목 조회 시 응답 시간을 크게 단축할 수 있다.

**대안:**
- 개별 쿼리 허용 → 지출이 많을 경우 응답 시간이 선형으로 증가
- SQLAlchemy 관계 로딩 최적화 → ORM 수준 복잡도 증가
- 단순 JOIN 쿼리 → 카테고리 중복 행 처리로 클라이언트 필터링 필요

**파일:** C:\Users\Jason\Desktop\dashboard\routers\expense.py
