# 프로젝트 결정 기록

---
## 2026-05-27 — 가계부 Phase 1 DB 설계: 카테고리 계층 + 환율 캐싱 + 안전 마이그레이션
**결정:** expense_categories (자기참조 계층), expense_budgets (월별/연간 예산), exchange_rates (9개 통화) 3개 신규 테이블 추가. expenses 테이블에 category_id, subcategory_id, currency, converted_amount, exchange_rate 컬럼 확장. 안전한 컬럼 추가를 위해 _migrate_expense_columns() 함수 + lifespan 자동 실행, 초기 환율 시드 _seed_exchange_rates()
**이유:** (1) 계층 구조 확장성 — 단순 텍스트 category는 일관성 유지 불가, 정규화된 자기참조 구조로 대분류/소분류 자유롭게 확장 가능. (2) 사용자 커스터마이징 — user_id=NULL(시스템 기본) vs user_id=INT(사용자 커스텀) 구분으로 기본 카테고리 제공하면서 맞춤형 추가 지원. (3) 환율 고정 — 거래 당시의 환율을 exchange_rate 컬럼에 저장하여 과거 거래 정확성 유지, API 의존성 제거. (4) 예산 관리 — expense_budgets로 월별 카테고리별 예산 설정 및 지출 대비 분석 가능. (5) 안전한 마이그레이션 — 기존 데이터 손실 없이 새 컬럼 추가, 서버 시작 시 자동 반영
**대안:** (1) JSON JSONB 컬럼 — 유연하지만 쿼리 최적화, 예산 집계 성능 저하. (2) 단일 카테고리 테이블 계층 없음 — 구현 단순하지만 소분류 확장 불가. (3) 다단계 정규화 테이블 분산 — 복잡도 증가, 조인 비용 증가. (4) 고정 환율만 사용 — 다중 통화 거래 불가, 글로벌 지원 제한
**파일:** CHANGELOG.md, models.py, main.py, DB_SCHEMA.md

---
## 2025-01-14 — 언어 상태 관리 전략 (localStorage + 이벤트 기반)
**결정:** LoginPage에서 언어 설정을 localStorage로 저장하고, 커스텀 'languageChanged' 이벤트를 통해 다른 컴포넌트의 언어 변경에 반응하도록 구현
**이유:** 브라우저 새로고침 후에도 사용자의 언어 선택이 유지되고, 앱의 여러 부분에서 언어 설정이 변경될 때 LoginPage가 자동으로 동기화되도록 하기 위함
**대안:** (1) React Context API를 통한 전역 상태 관리 — 더 큰 규모 앱에 적합하지만 초기 구성이 복잡함. (2) URL 매개변수 — SEO 친화적이지만 언어 전환 시 새로고침 필요. (3) 폴링 방식 — 성능 비효율적
**파일:** frontend/src/pages/LoginPage.jsx

---
## 2025-01-14 — RegisterPage에 언어 상태 관리 패턴 적용
**결정:** RegisterPage에도 동일한 localStorage + 커스텀 이벤트 기반 언어 상태 관리 패턴을 적용
**이유:** 회원가입 페이지도 로그인 페이지와 마찬가지로 언어 설정을 유지하고 동기화해야 하며, 이미 검증된 패턴을 일관되게 적용하기 위함
**대안:** (1) 각 인증 페이지마다 다른 상태 관리 방식 사용 — 일관성 부족. (2) 나중에 Context API로 통합 — 현재는 단일 패턴 유지가 우선. (3) 상태 관리 라이브러리 도입 — 현 단계에서는 과도한 복잡도
**파일:** frontend/src/pages/RegisterPage.jsx

---
## 2025-01-15 — SuperadminPage에 언어 상태 관리 패턴 확대
**결정:** SuperadminPage에도 동일한 localStorage + 커스텀 이벤트 기반 언어 상태 관리 패턴을 적용하여, 슈퍼어드민 페이지가 언어 변경 시 자동으로 동기화되도록 구현
**이유:** 모든 주요 페이지가 일관된 방식으로 언어 설정을 관리하도록 하고, 사용자가 언어를 변경할 때 슈퍼어드민 페이지도 즉시 반응하기 위함. 이미 LoginPage와 RegisterPage에서 검증된 패턴을 확대 적용
**대안:** (1) Context API 도입 — 더 확장 가능하지만 현재 단순한 구조에서는 과도. (2) Redux 같은 상태 관리 라이브러리 — 규모가 작은 프로젝트에서는 복잡도 증가. (3) 각 페이지별 고유 구현 — 중복 코드 야기
**파일:** frontend/src/pages/SuperadminPage.jsx

---
## 2025-01-15 — ALL_TZ 배열을 i18n 기반 labelKey 구조로 리팩토링
**결정:** AdminPage의 ALL_TZ 시간대 배열에서 하드코딩된 한글 label (예: '서울 (KST)')을 i18n 기반 labelKey 참조 (예: labelKey: 'tzSeoul')로 변경
**이유:** (1) 다국어 지원 확장 — 시간대 표시를 번역 시스템으로 관리하여 새로운 언어 추가 시 ALL_TZ 코드 수정 불필요. (2) 일관성 — 나머지 UI 요소들이 이미 t(lang, 'key') 패턴을 사용하고 있으므로 시간대도 동일 패턴 적용. (3) 유지보수성 — 번역 파일(ko.json, en.json)에서 모든 라벨을 중앙 관리
**대안:** (1) 인라인 t() 호출 — 배열 구조에서 함수 호출은 구조적으로 부자연스러움. (2) 상수 객체로 분리 — 현재 방식이 더 간단명료. (3) 계속 하드코딩 유지 — 다국어 지원 시 코드 복잡도 증가
**파일:** frontend/src/pages/AdminPage.jsx

---
## 2025-05-27 13:18 — SVG 아날로그 시계 구현 및 1초 단위 상태 동기화
**결정:** HeroSection 컴포넌트에 새로운 AnalogClock SVG 컴포넌트를 추가하고, 타이머를 10초 단위 tick에서 1초 단위 now 상태로 변경하여 초침이 부드럽게 움직이도록 구현
**이유:** (1) 사용자 경험 향상 — 아날로그 시계로 시간을 직관적으로 표현하고, 초침 애니메이션으로 시간이 흐르고 있음을 시각적으로 전달. (2) 상태 동기화 — 1초 단위 now 상태를 AnalogClock과 formatTZ() 함수에 공유하여 모든 시간대 표시가 정확히 동기화. (3) 유연한 확장성 — clockCount prop에 따라 동적으로 시계 크기 조정
**대안:** (1) 라이브러리 사용 (react-clock 등) — 외부 의존성 추가로 번들 크기 증가, 커스터마이징 제한. (2) CSS 애니메이션 — 정확한 초침 위치 계산 어려움, 실시간 동기화 불가. (3) 디지털 시계 유지 — 기존 방식이지만 아날로그 시계의 시각적 임팩트 손실
**파일:** frontend/src/pages/index/HeroSection.jsx

---
## 2025-05-27 — 가계부 Phase 1 DB 스키마 정규화 (문자열 카테고리 → 정규화 테이블)
**결정:** Expense 모델의 단순 텍스트 `category` 필드를 세 개의 정규화된 테이블(ExpenseCategory, ExpenseBudget, ExchangeRate)로 교체. ExpenseCategory는 계층 구조(대분류/소분류)와 시스템/커스텀 카테고리 구분을 지원하며, 다중 통화 지원을 위해 currency, converted_amount, exchange_rate 컬럼 추가
**이유:** (1) 확장성 — 텍스트 기반 카테고리는 일관성 유지 어렵고 쿼리 성능 낮음. 정규화된 테이블로 계층 조회, 필터링, 집계 효율화. (2) 다중 통화 지원 — 글로벌 사용자 대응 및 환율 기반 통일된 보고. (3) 예산 관리 — ExpenseBudget 테이블로 연/월별 카테고리 예산 설정 가능. (4) 하위 호환성 — 레거시 category 필드 유지로 기존 데이터 보존
**대안:** (1) JSON 컬럼 사용 — PostgreSQL JSONB는 유연하지만 쿼리 최적화 어려움, 집계 성능 저하. (2) 싱글 카테고리 테이블 (계층 없음) — 구현 단순하지만 향후 소분류 확장 불가. (3) NoSQL 전환 — 관계형 데이터(예산, 교환율)에는 부적합
**파일:** models.py

---
## 2025-05-27 21:36 — 환율 데이터 저장 전략 (hardcoded seed + 트랜잭션 단위 동결)
**결정:** 주요 통화 9쌍(USD↔KRW, EUR, JPY, GBP, CAD, AUD, CNY, HKD, SGD)에 대해 hardcoded _DEFAULT_EXCHANGE_RATES로 초기 시드하고, 각 지출 거래마다 exchange_rate 컬럼에 그 시점의 환율을 기록하는 방식 도입
**이유:** (1) 거래 정확성 — 과거 지출을 현재 환율로 재계산하지 않고 당시의 정확한 환율을 유지. (2) 오프라인 친화적 — 서버 시작 시 seed로 기본 환율을 로드하므로 API 의존성 제거. (3) 데이터 무결성 — 환율 변동으로 인한 보고서 왜곡 방지. (4) 간단한 구현 — 매 트랜잭션마다 외부 API 호출 불필요
**대안:** (1) 실시간 외부 API (exchangerate-api, fixer.io) — 최신 환율 반영하지만 API 장애 시 기능 마비, 비용 부담. (2) 매일 자동 갱신 (APScheduler) — 복잡도 증가, 과거 환율 기록 불가. (3) 사용자 입력 — 수동 오류 위험, UX 악화. (4) 고정 환율만 사용 — 다중 통화 지원 의미 감소
**파일:** main.py

---
## 2025-05-27 21:36 — 스키마 마이그레이션 방식 선택 (raw ALTER TABLE + inspect)
**결정:** Alembic 같은 마이그레이션 도구 대신, main.py의 lifespan 초기화 단계에서 _migrate_expense_columns() 함수로 직접 ALTER TABLE 실행하고, inspect()로 기존 컬럼 존재 여부 확인하는 방식 채택
**이유:** (1) 경량성 — Alembic은 소규모 프로젝트에서 오버헤드. 한 번 생성되고 거의 변경되지 않는 테이블에 불필요. (2) 배포 단순성 — 마이그레이션 버전 관리 없이도 컬럼이 없으면 자동 추가. (3) 멱등성 — inspect로 중복 추가 방지, 안전하게 재실행 가능. (4) 개발 속도 — 신규 컬럼은 코드 수정 후 자동으로 반영
**대안:** (1) Alembic — 복잡한 버전 관리, 배포 파이프라인 통합 필요. (2) SQLAlchemy ORM 기반 (automap) — 동적 리플렉션은 타입 안정성 약화. (3) Django 같은 프레임워크 — FastAPI 철학과 맞지 않음
**파일:** main.py

---
## 2026-05-27 — 가계부 카테고리 정규화: 계층 구조(대분류/소분류) 및 다중 통화 완전 지원
**결정:** expenses 테이블의 단순 텍스트 category 필드를 세 개의 새로운 정규화 테이블로 전면 교체: (1) expense_categories — 자기 참조 FK(parent_id)로 2단계 계층 구조 지원, 시스템 기본 카테고리(user_id=NULL)와 사용자 커스텀 카테고리 구분. (2) expense_budgets — 사용자별, 카테고리별, 연/월별 예산 설정. (3) exchange_rates — 9개 주요 통화 쌍의 USD 기준 환율 저장. expenses에 category_id, subcategory_id, currency, converted_amount, exchange_rate 컬럼 추가
**이유:** (1) 확장성 — 텍스트 카테고리는 일관성 유지 어렵고 쿼리 최적화 불가. 정규화된 구조로 계층 조회, 필터링, 집계 효율화. (2) 예산 관리 — 별도 테이블로 카테고리별·월별 예산 설정 가능, 실제 지출과 대비 분석. (3) 다중 통화 지원 — 글로벌 사용자 대응, 모든 거래를 USD 기준으로 통일 보고 가능. (4) 이중 언어 지원 — name_ko, name_en으로 한영 지원, 향후 다국어 확장 용이. (5) 하위 호환성 — 레거시 category 필드 유지로 Phase 1 이전 데이터 보존
**대안:** (1) 단일 카테고리 테이블(계층 없음) — 구현 단순하지만 향후 소분류 확장 불가, 예산 설정 불가. (2) JSON 컬럼(JSONB) — PostgreSQL JSONB는 유연하지만 쿼리 최적화 어려움, 환율·예산 집계 성능 저하. (3) NoSQL 전환 — 관계형 데이터(예산, 환율, 계층)에는 부적합. (4) 싱글 카테고리 FK 유지 — 대분류만 지원, 소분류 분석 불가
**파일:** DB_SCHEMA.md
